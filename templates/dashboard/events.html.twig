{% extends 'base_back.html.twig' %}

<<<<<<< HEAD
{% block body %}
    
    <h1>Events</h1>

    <!-- Bouton flottant pour ajouter un événement -->
    <a href="{{ path('app_events_new') }}" class="btn btn-primary floating-button" title="Ajouter un événement">
        <i class="fas fa-plus"></i>
    </a>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Title</th>
                <th>Description</th>
                <th>Lieu</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for event in events %}
                <tr>
                    <td>{{ event.id }}</td>
                    <td>{{ event.title }}</td>
                    <td>{{ event.description }}</td>
                    <td>{{ event.lieu }}</td>
                    <td>{{ event.date|date('Y-m-d H:i') }}</td>
                    <td>

                        <a href="{{ path('app_events_edit', {'id': event.id}) }}" class="btn btn-info">Modifier</a>
                         <a href="{{ path('app_events_show', {'id': event.id}) }}" class="btn btn-info">details</a>
                        {{ include('events/_delete_form.html.twig', {'event': event}) }}
                    </td>
                      {% if event.image %}
                                <img src="{{ asset('uploads/images/' ~ event.image) }}" alt="Event Image" width="150" class="img-thumbnail">
                            {% else %}
                                <span class="text-muted">Aucune image</span>
                            {% endif %}
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <!-- Ajout du CSS pour positionner le bouton -->
    <style>
        .floating-button {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            font-size: 24px;
        }
    </style>

    <!-- Ajout de FontAwesome pour les icônes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
=======
{% block title %}home -{% endblock %}

{% block body %}



>>>>>>> 8ba3048163ae6bc316db3eb80ca3688bc7ea3320

{% endblock %}