{{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}

<div class="form-group">
    {{ form_label(form.nom) }}
    {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Enter nom'}}) }}
    <div class="text-danger">
        {{ form_errors(form.nom) }}
    </div>
</div>

<div class="form-group">
    {{ form_label(form.description) }}
    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Enter description'}}) }}
    <div class="text-danger">
        {{ form_errors(form.description) }}
    </div>
</div>

<div class="form-group">
    {{ form_label(form.type_analyse) }}
    {{ form_widget(form.type_analyse, {'attr': {'class': 'form-control', 'placeholder': 'Enter type_analyse'}}) }}
    <div class="text-danger">
        {{ form_errors(form.type_analyse) }}
    </div>
</div>

<div class="form-row row">
    <div class="form-group col-md-4">
        {{ form_label(form.date_mise_a_jour) }}
        {{ form_widget(form.date_mise_a_jour, {'attr': {'class': 'form-control', 'placeholder': 'Enter date mise a jour'}}) }}
        <div class="text-danger">
            {{ form_errors(form.date_mise_a_jour) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.statut) }}
        {{ form_widget(form.statut, {'attr': {'class': 'form-control', 'placeholder': 'statut'}}) }}
        <div class="text-danger">
            {{ form_errors(form.statut) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.date_creation) }}
        {{ form_widget(form.date_creation, {'attr': {'class': 'form-control', 'placeholder': 'Enter date_creation'}}) }}
        <div class="text-danger">
            {{ form_errors(form.date_creation) }}
        </div>
    </div>
</div>

<div class="form-group">
    {{ form_label(form.prix) }}
    {{ form_widget(form.prix, {'attr': {'class': 'form-control', 'placeholder': 'Enter prix'}}) }}
    <div class="text-danger">
        {{ form_errors(form.prix) }}
    </div>
</div>

<div class="form-group">
    {{ form_label(form.laboratoire) }}
    {{ form_widget(form.laboratoire, {'attr': {'class': 'form-control', 'placeholder': 'Enter responsable'}}) }}
    <div class="text-danger">
        {{ form_errors(form.laboratoire) }}
    </div>
</div>

<div class="form-group">
    {{ form_label(form.rendezVous) }}
    {{ form_widget(form.rendezVous, {'attr': {'class': 'form-control', 'placeholder': 'Enter responsable'}}) }}
    <div class="text-danger">
        {{ form_errors(form.rendezVous) }}
    </div>
</div>
 <div class="text-center">
                        <button type="submit" class="btn btn-success px-5">
                            <i class="fas fa-save"></i> Enregistrer
                        </button>
                    </div>



{{ form_end(form) }}

<script>
    document.addEventListener('DOMContentLoaded', function () {
        // Sélectionner tous les champs de formulaire
        const formFields = document.querySelectorAll('.form-control');

        // Appliquer l'écouteur d'événement 'input' à chaque champ
        formFields.forEach(function(field) {
            field.addEventListener('input', function () {
                // Vérifier la validité du champ
                if (field.validity.valid) {
                    // Si valide, ajouter is-valid et retirer is-invalid
                    field.classList.remove('is-invalid');
                    field.classList.add('is-valid');
                } else {
                    // Si invalide, ajouter is-invalid et retirer is-valid
                    field.classList.remove('is-valid');
                    field.classList.add('is-invalid');
                }
            });
        });
    });
</script>
